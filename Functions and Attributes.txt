Function
void moveLeft(int numPix, Obj myObj)
void moveRight(int numPix, Obj myObj)
void jump(int numPix, Obj myObj)
shoot()
crouch() //possible
reload()
interact()
discardItem()
accessInventory()
switchItem()
activate ability
use item
melee attack
Escape

Flesh out functions with method header and comments explaining what each part does


Attribute
collide
visible
moveable
isPlayer
destroyable

loop through collidable array and check to see collision
add pair to collision vector
give each object unique id

<name>
Bob
<visible>
	<XPos>
	0
	<YPos>
	0
	<Spritesheet>
	foo
</visible>
<colliodable>

create function generates objects before loop
